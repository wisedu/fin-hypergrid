# folder generated by npm
/node_modules

# folder generated by `browserify -do build; browserify -o | uglify -o build`
/build

# ignore possible garbage left behind by MS Office
~*.*x

# keep /demo in here even though there's just so it could be copied in and the IDE will see it during development
/fin-hypergrid-demos
/fin-hypergrid-loader-demo
